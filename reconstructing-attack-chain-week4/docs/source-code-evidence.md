# Source-Code Evidence

The Week 4 materials identified an application search route that disables the normal API-key requirement and builds a SQL query using direct string interpolation of the `q` parameter.

For the public repository, do not publish private project files or credentials. If source code is included, remove secrets and publish only the minimum safe code necessary to demonstrate the vulnerability.
